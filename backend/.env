# Server configuration
PORT=3000
NODE_ENV=development

# Database configuration (empty defaults to Mock DB for local testing)
DATABASE_URL=
DB_SSL=false

# Frontend configuration
FRONTEND_URL=http://localhost:5173
